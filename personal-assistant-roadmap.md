# 高级个人助手实现路线图

这份文档记录 Personal Dashboard 从普通工具面板升级为“更懂你的 AI 助手”的实现步骤。目标不是一次性做一个复杂 Agent，而是按层搭地基：先让系统可靠观察行为，再保存完整内容，再提炼长期记忆，最后才做 RAG 检索、习惯推断和主动建议。

## 总目标

高级个人助手需要长期理解三类东西：

- 行为习惯：你什么时候专注、什么时候拖延、常在哪些时间切换模式、哪些任务经常被推迟。
- 工作方式：你怎么拆任务、怎么使用待办/日程/便签/番茄钟/音乐/日历，以及你和 AI 的协作偏好。
- 当前上下文：今天有哪些任务、哪些会议、哪些项目在推进、哪些笔记和对话值得继续。

最终希望它能做到类似：

- “帮我把这个任务推迟到明天。”
- “把面板改成专注模式，隐藏音乐。”
- 番茄钟结束后自动生成回顾摘要。
- 检测到日历有会议后，在便签里准备议程模板。
- 周五下午通常进入轻松模式时，提前给出建议。

## 核心分层

不要把所有数据都塞进一个 memories 表。更稳定的架构是三层：

```text
事件表
  记录发生了什么、什么时候、在哪个组件、摘要、结构化 metadata

内容表 / 当前状态
  记录完整便签、完整 AI 对话、完整任务内容、完整项目资料

记忆表 / RAG 索引
  记录从事件和内容中提炼出来的长期偏好、习惯、总结、embedding
```

这三层解决的问题不同：

- 事件表是证据层，回答“过去发生过什么”。
- 内容表是状态层，回答“现在完整内容是什么”。
- 记忆表是认知层，回答“从长期历史里学到了什么”。

## 第一步：行为事件层

状态：已实现。

这一层做“观察能力”，不做主动决策。

实现内容：

- 前端所有主要组件产生统一格式的行为事件。
- 事件先写入本地队列 `primoria-behavior-events-queue.v1`。
- 登录后每 60 秒批量同步 Supabase，每批最多 50 条。
- Supabase 使用独立表 `user_behavior_events` 保存事件。
- 用 `client_event_id` 做幂等，避免重复上传。
- 用 RLS 保证登录用户只能访问自己的事件。
- 事件保存摘要和结构化 metadata，不保存完整正文、signed URL、data URL 或 iframe HTML。

为什么先做它：

RAG 需要可检索的材料，习惯推断需要真实行为样本，主动建议需要知道用户过去怎么做。如果没有事件层，AI 只能根据当前页面猜测，无法形成长期理解。

## 第二步：内容表 / 当前状态层

状态：下一阶段。

这一层保存完整内容，而不是只保存摘要。

适合放在内容层的数据：

- 完整便签和格纸笔记。
- 完整任务标题、描述、状态、日期、排序。
- 完整 AI 对话记录。
- 日历读取结果和每日简报结果。
- 项目资料、会议议程、长期目标。

内容层和事件层的区别：

```text
事件层：你在 10:30 把任务 A 推迟到明天。
内容层：任务 A 的完整标题、描述、当前日期、所属项目。
```

事件层不应该复制完整内容，因为它会产生大量中间版本。内容层负责保存权威状态，事件层负责保存变化过程。

## 第三步：每日/每周反思层

状态：待实现。

这一层把原始事件变成可学习的摘要。

输入：

- `user_behavior_events`
- 当前任务、日程、习惯、番茄钟、便签、AI 对话

输出：

- 今日完成了什么。
- 哪些任务被反复推迟。
- 哪些时间段最容易进入专注。
- 哪些组件使用频率最高。
- 哪些 AI 建议被接受或取消。
- 明天最值得优先处理的事项。

这一层可以先做定时或手动触发，不需要一开始就全自动。

## 第四步：长期记忆层

状态：待实现。

这一层把反思摘要进一步提炼成稳定记忆。

记忆类型建议分开：

- 用户偏好：例如“用户周五下午更偏向轻松模式”。
- 工作习惯：例如“用户常把未完成任务推迟到第二天早上处理”。
- 项目事实：例如“Dashboard 的长期方向是个人 AI 助手”。
- 程序性规则：例如“自动执行前必须先创建 pending action 并等待确认”。

每条长期记忆都需要记录：

- 内容
- 来源证据
- 置信度
- 更新时间
- 是否过期
- 适用范围

长期记忆不应该只追加不更新。后续需要冲突处理，例如新证据和旧偏好相反时，要降低旧记忆置信度或生成新版本。

## 第五步：RAG 检索层

状态：待实现。

RAG 不直接检索所有原始事件，而是优先检索长期记忆、反思摘要和当前内容。

推荐检索顺序：

```text
用户当前请求
  -> 检索当前状态
  -> 检索相关长期记忆
  -> 检索必要的原始事件证据
  -> 注入给 LLM
  -> 生成回答或 pending action
```

这样可以避免把大量低价值事件塞进上下文，也能减少幻觉。

向量数据库可以后置选择：

- Supabase pgvector：适合跨设备、和现有 Supabase 数据靠近。
- Caliby：适合未来本地优先、低延迟、离线检索。

第一阶段不绑定向量库是有意的。只要事件和内容格式稳定，未来可以同时接 pgvector 或 Caliby。

## 第六步：主动建议层

状态：待实现。

主动建议不是自动替用户做事，而是基于记忆和当前上下文提出建议。

例子：

- 番茄钟结束后，建议生成本轮回顾。
- 会议前，建议创建议程模板。
- 发现任务连续多天推迟，建议拆小或移动到更合适时间。
- 周五下午，建议切换轻松模式。
- 每天早上，建议今日优先级。

主动建议必须区分两类动作：

- 低风险：只展示建议、生成摘要、提示用户。
- 高风险：改任务、删内容、改布局、发送消息，必须走 pending action 确认。

## 第七步：主动执行层

状态：更后期。

这一层才接近“贾维斯式助手”。

原则：

- AI 可以计划，但高风险动作不能直接执行。
- 所有执行都要留下事件记录。
- 每次执行后要能回滚或解释。
- 用户确认过的模式可以逐步提高自动化程度。

例如：

```text
检测到会议
  -> 读取日历标题和时间
  -> 检索相关项目记忆
  -> 生成议程模板
  -> 创建 pending action
  -> 用户确认
  -> 写入便签
  -> 记录执行事件
```

## 当前实现位置

已经完成的地基：

- `user_behavior_events`：Supabase 独立行为事件表。
- `src/lib/behaviorEvents.ts`：事件清洗、入队、队列管理。
- `src/lib/behaviorEventSync.ts`：批量上传、幂等处理、失败保留。
- `src/hooks/useBehaviorEventSync.ts`：登录后节流同步。
- `learning.md`：第一步事件层的学习笔记。

下一步最适合做：

1. 设计内容表，把完整 AI 对话、便签、任务内容从事件层分离出来。
2. 做每日反思任务，从 `user_behavior_events` 生成 daily summary。
3. 建长期记忆表，先不接 embedding，只保存人工可读的结构化记忆。
4. 再接 RAG 检索，把当前请求相关的记忆注入 AI 助手。

## 关键设计原则

- 先观察，再记忆，再推理，再主动。
- 事件是证据，不是完整内容仓库。
- 长期记忆是提炼结果，不是原始日志堆积。
- 主动建议先做“建议”，再做“确认后执行”。
- 每个自动化动作都要可解释、可追踪、可撤销。

